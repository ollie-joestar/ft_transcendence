# .env.example (root)
# ------------
# Postgres container initialisation credentials.
# Used by:
# - db service (docker-compose.yml -> db -> env_file: .env)
# - vault-init service to construct DATABASE_URL for Vault seeding
# Keep only non-secret local defaults here.

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=tr_db
